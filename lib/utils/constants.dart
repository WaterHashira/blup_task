import 'package:flutter/animation.dart';
import 'package:flutter/material.dart';

const Offset initialBoxPosition = Offset(50, 100);

const double boxMaxHeight = 50.0;
const double boxMaxWidth = 100.0;

const minClosureLength = 40;
